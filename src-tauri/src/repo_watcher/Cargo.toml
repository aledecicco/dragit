[package]
name = "repo_watcher"
version = "0.1.0"
edition = "2021"


[dependencies]
tauri = { workspace = true }

models = { path = "../models" }

notify = { version = "6.1.1" }
notify-debouncer-full = { version = "0.3.1" }
