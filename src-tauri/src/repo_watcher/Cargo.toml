[package]
name = "repo_watcher"
edition = "2021"


[dependencies]
tauri = { workspace = true }

models = { path = "../models" }
utils = { path = "../utils" }

notify = { version = "8.2.0" }
notify-debouncer-full = { version = "0.7.0" }
